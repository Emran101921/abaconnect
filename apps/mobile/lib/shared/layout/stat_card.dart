export '../widgets/app_stat_card.dart' show AppStatCard;

import '../widgets/app_stat_card.dart';

/// Alias for [AppStatCard] — consistent naming in the design system.
typedef StatCard = AppStatCard;
